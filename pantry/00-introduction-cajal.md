# CAJAL Figure Intelligence — Introduction

**Source:** `chapters/00-introduction.md`
**Mode:** `/scan silent`
**Domain note:** Layout-purpose palette. Primary structural Blue `#0072B2`, Secondary Sky Blue `#56B4E9`, Highlight Orange `#E69F00`, Contrast Vermillion `#D55E00`, Reddish Purple `#CC79A7`, neutral light gray.

---

## Density Recommendation

**2 figures, Foundational density.** The book-map figure (already referenced as Fig 0.1) and the WestEd ASSISTments treatment-vs-control contrast. The introduction is a roadmap — additional figures risk pre-empting the chapters they belong to.

---

## Zone Map

- **MC:** The book's central move (every "ban everything" finding is a finding about *no skilled human in the loop*) is mechanism-shaped but the chapter explicitly says it will recur throughout — let the chapters that name it again carry the visual.
- **VG:** The three-movement architecture and the AI+1 teacher thread weaving through six specific chapters.
- **PQ:** The WestEd RCT result — treatment classrooms produced statistically significant gains, control produced no measurable difference. Two conditions, two outcomes. This is the chapter's spine and it is structurally a comparison.

---

## Figure 0.1 — The Shape of the Argument (Book Map)

**Priority: Critical.** Already referenced in the draft. The introduction promises a thread that runs through chapters 2, 5, 6, 8, 9, and 12 — the reader needs to see the path.

### Block 1 — Illustrae paste block

A single horizontal composition divided into three columns by thin Black `#000000` 0.5pt vertical rules. Each column represents one of the three movements. Inside each column, a vertical stack of small rectangular chapter nodes — 3 nodes in column 1 (Movement I: chapters 1–3), 4 nodes in column 2 (Movement II: chapters 4–7), 5 nodes in column 3 (Movement III: chapters 8–12). All chapter nodes Sky Blue `#56B4E9` filled, equal width within columns. Overlaid across all three columns, a single Orange `#E69F00` 1pt dashed line traces from the second node in column 1 through the second and fourth nodes in column 2 and the first, second, and last nodes in column 3 — the AI+1 teacher thread (chapters 2, 5, 6, 8, 9, 12). The dashed line is the visual focus. White background, flat vector, double-column 174mm preferred.

### Block 2 — Full SCOPE prompt

[S] Double-column 174mm preferred, vector, white background.
[C] Three vertical columns representing three movements. Column 1 = 3 chapter nodes. Column 2 = 4 chapter nodes. Column 3 = 5 chapter nodes. One Orange dashed thread crossing all three columns, touching specific chapter nodes (positions 2 in col 1; 2 and 4 in col 2; 1, 2, and 5 in col 3).
[O] Three vertical columns, left-to-right reading order. Thin vertical rules separate columns. Chapter nodes stacked vertically inside each column. Dashed thread overlays the columns at specified node positions.
[P] Chapter nodes Sky Blue `#56B4E9` filled. AI+1 thread Orange `#E69F00` 1pt dashed. Column rules Black `#000000` 0.5pt. White background. Flat vector.
[E] No text labels in image, no chapter numbers, no movement names, no human figures, no thread arrows (the thread is non-directional, not a sequence), no decorative ornament, no shadows, no gradient fills.

### Block 3 — Negative prompt

text labels, words, chapter numbers, movement names, gibberish letters, titles, captions, human figures, thread arrows, decorative ornament, photographic elements, realistic 3D textures, plastic wrap effects, drop shadows, gradient backgrounds, gradient fills, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, overlapping unaligned paths, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 0.2 — WestEd ASSISTments: Treatment vs. Control

**Priority: Critical.** PQ. The chapter opens and closes on this trial; one bar chart anchors the argument.

### Block 1 — Illustrae paste block

A bar chart with two vertical bars on a horizontal x-axis. Y-axis starts at exactly zero with a single tick mark for a reference threshold (the no-effect baseline). Bar 1 (Treatment: trained teachers): Blue `#0072B2` filled, taller than the threshold marker. Bar 2 (Control: untrained teachers, same software): Sky Blue `#56B4E9` filled, drawn at the height of the threshold marker — visually flush with zero-gain. A thin horizontal Black `#000000` 0.5pt dashed line runs across the plot at the threshold level — *no measurable difference*. Y-axis label region blank (post-applied typography). X-axis label region blank. White background, flat vector, single-column 89mm.

### Block 2 — Full SCOPE prompt

[S] Single-column 89mm, vector, white background, 300 DPI fallback.
[C] Bar chart with 2 vertical bars (Treatment = trained teachers; Control = untrained teachers, same software). Treatment bar significantly taller than control. Horizontal dashed reference line at the no-effect threshold.
[O] Vertical bars on horizontal x-axis. Y-axis on left, starting at zero. Dashed reference line spans the plot horizontally. No gridlines.
[P] Treatment bar Blue `#0072B2` filled. Control bar Sky Blue `#56B4E9` filled. Axis lines Black `#000000` 1pt. Reference line Black 0.5pt dashed. White background. Flat vector.
[E] No data labels above bars, no numerical labels on axes, no chart title, no legend, no third bar, no error bars (the chapter does not provide confidence intervals to render honestly), no broken-axis indicators, no 3D bar effects, no shadows, no gradient fills.

### Block 3 — Negative prompt

text labels, words, data labels above bars, percentage labels, axis numerical labels, chart titles, legends, error bars, third bars, gridlines inside plot, broken-axis indicators, 3D bar effects, drop shadows, gradient fills, gradient backgrounds, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Video Candidate Pass

**FIGURE 0.1 (Book map):** STATIC SUFFICIENT. Architectural overview, not transitional.
**FIGURE 0.2 (Treatment vs. control):** STATIC SUFFICIENT. Bar charts are read at a glance.

**Video candidates identified: 0.** No production recommended.

---

## Split-point note

The "what would change my mind / still puzzling" framing the chapter introduces is a typographic pattern that appears throughout the book — handle as a styled callout box in the design pass, not a CAJAL figure. The vocabulary list (*implementation gap*, *cognitive offloading*, *fluency trap*, *AI dividend*, *within-thirty argument*, *educational exemption*, *AI+1 teacher*, *three honest measures*) is the book's glossary — also typography, not a figure.
